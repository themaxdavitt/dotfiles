# Agent Guidance

This repo holds an example project, designed to enable writing examples, etc.

## Development process

- ALWAYS: read `README.md` and `CONTRIBUTING.md` first, then set up your environment, e.g.: `<one-canonical-install-command>`
- ALWAYS: ensure quality checks pass before considering the task done, e.g., via:
  - Test: `<command-A>`
  - Lint: `<command-B>`
  - Format: `<command-C>`
  - Full workflow: `<command-D>`

## Review and verification

- ALWAYS: run `<targeted-tests>` before finalizing changes.
- ALWAYS: keep changes scoped to the issue requirements.
- NEVER: run the full test matrix unless the issue's acceptance criteria require it.
- NEVER: refactor unrelated code unless needed for a fix.
