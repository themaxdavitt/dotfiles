You are writing the `# Core Directives` section of a `SKILL.md` whose stated purpose is this team's Git commit and branching conventions. A colleague has handed you five candidate rules to include. Produce the finished section.

- commit subjects use the imperative mood and stay under 72 characters
- every user-supplied string is parameterised before it reaches the database
- feature branches rebase onto main rather than merging it back in
- a commit that touches more than one concern is split before review
- the branch name carries the ticket identifier as its first segment
