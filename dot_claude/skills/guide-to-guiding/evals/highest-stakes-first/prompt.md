Assemble the `# Core Directives` section of a `SKILL.md` from these five approved rules. Keep all five, reproduce their wording, and present the finished section.

- trailing whitespace is stripped from every file before it is saved
- migrations that drop a column never run against the production database without a signed-off backup
- module filenames use lowercase with underscores
- every exported function carries a docstring
- import blocks are ordered standard library, third party, local
