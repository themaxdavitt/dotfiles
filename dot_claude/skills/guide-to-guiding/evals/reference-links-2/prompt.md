Write `AGENTS.md` for our internal service repo. Agents working here regularly need three documents: the deploy runbook (https://wiki.corp.example/deploy-runbook), the API style guide (https://wiki.corp.example/api-style), and the on-call escalation matrix (https://wiki.corp.example/escalation). Several rules will need to point at more than one of them. Output only the `AGENTS.md` file contents.
