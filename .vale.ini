StylesPath = .vale
MinAlertLevel = suggestion

Vocab = House

# TODO: re-activate and make "House" style public

[*.{md}]
# BasedOnStyles = Vale, House
BasedOnStyles = Vale, HouseSubset

[{**/SKILL.md,**/CLAUDE.md,**/CLAUDE.local.md,**/AGENTS.md,**/AGENTS.local.md,**/AGENTS.override.md,**/.github/copilot-instructions.md}]
# BasedOnStyles = Vale, House, AgentGuidance
BasedOnStyles = Vale, HouseSubset, AgentGuidance

# Skill references/ hold on-demand documents (e.g. specs quoted from upstream,
# maintenance rituals) — they are read on demand, not authored guidance.
# TODO: this blanket exemption is expedient, not principled — non-SKILL agent
# guidance docs (e.g. guide-to-linting/references/troubleshooting.md) currently
# escape every lane; someday give their structure/prose its own checks.
[**/skills/*/references/*.md]
BasedOnStyles =

# Gitignored personal scratch (agent ledger) — never publishable prose, but hk
# can still hand it to Vale (e.g. while a staged deletion leaves it on disk).
[TODO*]
BasedOnStyles =
