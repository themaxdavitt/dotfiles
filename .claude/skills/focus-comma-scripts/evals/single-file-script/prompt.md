You are porting a small Node project into this dotfiles repo so it can run as a personal command. Upstream it is seven modules under `src/` totalling about 1000 lines — colour math, constants, a contrast check, data loading, two exporters, and an entry point — and roughly half of it produces outputs this machine will never use. Describe the file layout you would create in the repo.
